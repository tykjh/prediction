// MagicHeader.jsx renders no literal text of its own — title/subtitle/icon are all
// passed in as props by the parent component (e.g. Workspace.jsx, which owns its
// own strings via workspace.js). This dict file is created for consistency with the
// i18n architecture and is available if MagicHeader ever needs its own copy.
export const magicHeader = {
    en: {},
    zh: {},
};
